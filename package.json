{
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n \"api,web\" -c \"cyan,magenta\" \"tsx api/server.ts\" \"pnpm --filter frontend dev\"",
    "dev:api": "tsx api/server.ts",
    "dev:web": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "preview": "pnpm --filter frontend preview"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "@vercel/node": "^12.0.3",
    "concurrently": "^10.0.5",
    "tsx": "^4.23.13"
  }
}
